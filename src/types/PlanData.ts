// src/types/PlanData.ts
export interface PlanData {
    backlogResponsible: string;
    roadMap: string;
    deliverables: string;
    status: string; // Ej: "Good", "Warning", "Bad", "Not Defined"
    observations: string;
  }
  